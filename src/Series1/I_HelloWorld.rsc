module Series1::I_HelloWorld

import IO;

/*
 * Hello World & Basic IO
 *
 * Welcome to Rascal! 
 *
 * This series will guide you through the basics of the language.
 * We start with the classic "Hello World".
 *
 * 1. Open the console (click "Import in new Rascal Terminal" or right-click the file -> "Run As" -> "Rascal Application").
 * 2. In the terminal, type `import Series1::I_HelloWorld;` (if not already imported).
 * 3. Call the function `helloWorld()`.
 */

/*
 * Task:
 * 1. Use the `println` function from the `IO` module to print a string.
 */
void helloWorld() {
  // Fill in here
}

/*
 * Try this:
 * - Change the message.
 * - Print multiple lines.
 * - Use string interpolation: `println("Hello <name>!");` where `str name = "World";`
 */
